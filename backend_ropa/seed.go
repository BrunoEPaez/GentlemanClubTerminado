package main

import "fmt"

func SeedDatabase() {
    // Hemos quitado la lógica de creación para que la base de datos inicie vacía
    fmt.Println("Saltando carga de productos de prueba (Base de datos vacía)...")
}